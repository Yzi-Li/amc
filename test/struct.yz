; Must have a white space
;    here v
struct Str =>
	len: i32
	siz: mut i32
	s: mut *i32

fn main: i32 =>
	let test: mut Str = { 0, 1, null }
	let a: i32 = test.siz
	test.siz += 1
	test.s = &test.siz
	let b: *i32 = &a
	ret *test.siz
